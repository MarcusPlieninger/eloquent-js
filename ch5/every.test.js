




describe('everyLoop()', () => {
    test('examples from text', () => {
      expect(every([1, 3, 5], n => n < 10)).toEqual(['3', '2', '1'])
      expect(every([2, 4, 16], n => n < 10)).to
  

  })